<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Episodes</title>
    <script src="https://cdn.jsdelivr.net/npm/axios@1.6.7/dist/axios.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
<!-- <style> @media screen and (max-width: 667px) {
    body {
        overflow-x: hidden !important;
    }
    .container {
        max-width: 100% !important;
        overflow-x: hidden !important;
    }
}</style> -->
   

</head>

<body class="d-flex flex-column vh-100  " >

    
    <!-- <p>hhkhkjh</p>
    <p><%=id%></p> -->
    <div id="container" class="col"> 

    </div>
    <script>
          second();
        async function second(){
            // const rest=await axios.get(` https://api.tvmaze.com/shows/<%=id%>/episodes`)  it is all episodes

            const rest=await axios.get(`https://api.tvmaze.com/seasons/<%=id%>/episodes`);
            console.log(rest.data);
            showepisodes(rest.data);
           
            // 
           

            
            
            
            // console.log(rest.data)
        }
        const showepisodes=(episodes)=>{
let i=0;
for(let episode of episodes){
    i++;
    const para=document.createElement('div');
    var br = document.createElement("br");
    const text=document.createTextNode(`episode ${i} :`);
         para.style="width: auto;height:auto;  "
            para.className = " border border-2"
    para.append(text)
  
    // var br = document.createElement("br");
    // para.append(br)
    
    if(episode.image){
                    // console.log(season.image.medium)
                    const img=document.createElement('IMG');
                    img.src=episode.image.medium;
                            img.className="d-block w-10"
                    img.style="height: auto; width: 10rem; float: left; margin: 0 0 0 15px;"
                    para.append(img);
                }
               
                // const rating=document.createTextNode(episode.rating.average);
                const name=document.createTextNode(episode.name);
                para.append(name)
                para.append(br)
                let sp=document.createElement('span')

                let textnode2 = (episode.summary);
            if(textnode2){
                const tagRegExp =  new RegExp('<\s*[^>]*>', 'g')




textnode2 = textnode2.replace(tagRegExp, '');

console.log(textnode2)
            }
            const textnode22=document.createTextNode(textnode2)
          sp.append(textnode22)
          sp.style="width:50rem;"
          var br = document.createElement("br");
          para.append(br)
                const text2=document.createTextNode(episode.runtime);
           
             
                let d=document.createElement('span');
                var gent=document.createTextNode(" Duration :");
                var gent2=document.createTextNode(" minutes");
d.append(gent)
d.append(text2);
d.append(gent2)
// d.style="margin: 0 0 0 50px;"
                var br = document.createElement("br");
                // para.append(br)
                para.append(sp);
                var br = document.createElement("br");
                para.append(br)
                // para.append(br)
                var br = document.createElement("br");
                para.append(br)
                para.append(d)
                document.getElementById("container").appendChild(para)
}


        }
    </script>
     <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
</body>
</html>